# Nyckla — Parked Work & Notes

> Running list of ideas, deferred features, and things to revisit.
> Update this file whenever work is parked mid-session.

---

## Parked Features

### Key Return Flow
- No UI exists for returning a borrowed key
- `IssueRecord.returnedDate` is in the schema and ready
- Need: a "Return Key" action/button on the active-loans / borrower detail view
- Server action stub likely needed in `app/actions/issueKey.ts` or similar

### Settings — Display Names & Area Mappings
- Steps 5 (Display Names) and 6 (Area Mappings) were removed from the onboarding wizard to keep it lean
- Draft fields still exist in the onboarding schema for persistence
- Deferred to a future Settings tab (alongside the existing Organization / Access Areas tabs)

### Key Detail Page
- The keys table has a `viewLink` column ("View →") per key copy
- Unclear whether a full detail/history page exists for individual key copies
- Needs investigation: does `/keys/[id]` route exist? If not, build it.

### Overdue Notifications
- The active-loans timeline chart shows overdue buckets visually
- No automated alerts when a loan goes overdue (email, in-app, etc.)
- Would require a cron/scheduled job + Resend integration

---

## Technical Notes

### `next-env.d.ts` modified
- Shows as modified in git status from session start
- Likely auto-generated by Next.js — verify it's not carrying unintended changes before merging

### Double-Encryption Pattern (borrower PII)
- `createBorrowerWithAffiliation` manually encrypts PII before writing
- The Prisma extension in `lib/prisma-encryption.ts` also intercepts writes and skips if already encrypted
- Do NOT remove manual encryption from `createBorrowerWithAffiliation`

---

## i18n Phases Completed

| Phase | What | PR |
|---|---|---|
| 1 | Initial next-intl setup, routing | #15 |
| 2 | Auth, nav, org switcher | #15 |
| 3 | Dashboard, keys, settings, pagination | #17 |
| 4 | Issue-key workflow, onboarding, borrower form | pending |

Namespaces in use: `nav`, `org`, `auth`, `pagination`, `activeLoans`, `charts`, `keys`, `settings`, `issueKey`, `onboarding`, `borrowerForm`
